const { employees } = require("../data.js");
// Find projects with only one employee assigned

// Function Name: getSoloProjects
// Input: employees
// Output: ["Project Zeta"]
// Expected Result: Returns an array of project names with only one employee assigned.